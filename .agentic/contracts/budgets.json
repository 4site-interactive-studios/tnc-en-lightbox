{
  "bundleSize": {
    "_doc": "Gzip-size budget for dist/en-lightbox.js. The bundle-size contract (check_size.mjs) fails if the gzip size exceeds maxGzipBytes. Baseline: 1809B gzip (wave-0). Wave-1 measured: 2768B gzip. Re-baseline after wave-2 themes (Decision D-size).",
    "maxGzipBytes": 2800
  }
}
